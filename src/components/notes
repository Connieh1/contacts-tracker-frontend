this.state = {
      // count: 0,
      // clicked: false,
    };
  }

  //  handleClick = (id) => {
  //     this.state.clicked == false
  //       ? this.setState({ count: (this.state.count += 1) })
  //       : this.setState({ count: (this.state.count -= 1) });
  //     this.setState({
  //       clicked: !this.state.clicked,
  //     });
  //   };